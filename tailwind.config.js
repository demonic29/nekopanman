/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], // This should include all HTML files in `src` and subfolders
  theme: {
    extend: {},
  },
  plugins: [],
}
